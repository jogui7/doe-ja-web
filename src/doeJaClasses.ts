import { css } from '@emotion/react'

const doeJaClasses = {
  blankPageContainer: css({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  }),
}

export default doeJaClasses
